# Never copy the host's dependencies into the image — they would overwrite the
# ones installed inside it, including Playwright's browser bindings.
node_modules
**/node_modules

# Build output is produced inside the image.
server/dist
web/dist
*.tsbuildinfo

.git
.gitignore
.env
.env.*
!.env.example
*.log
README.md
sample-data
